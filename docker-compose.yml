services:
  nginx:
    build:
      context: .
      dockerfile: docker/Dockerfile.nginx
    ports:
      - "80:80"
    depends_on:
      node:
        condition: service_started
      ml:
        condition: service_started
    restart: unless-stopped

  node:
    build: ./nodeserver
    environment:
      PORT: 3001
      FRONTEND_URL: ${FRONTEND_URL:-http://localhost}
    restart: unless-stopped

  ml:
    build: ./mlserver
    environment:
      MODEL_PATH: /app/shape_classifier.tflite
      FRONTEND_URL: ${FRONTEND_URL:-http://localhost}
    volumes:
      - ./mlserver/shape_classifier.tflite:/app/shape_classifier.tflite:ro
    restart: unless-stopped
